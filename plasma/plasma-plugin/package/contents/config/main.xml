<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd" >
  <kcfgfile name=""/>

  <!-- No entries: wallpaper selection is owned entirely by the WP Linux
       app's own library/monitors_config, not Plasma's config system (see
       config.qml). This file is kept only because KPackage's
       Plasma/Wallpaper package structure expects config/main.xml to
       exist. ⚠ Confirm on real hardware that kpackagetool6 still
       installs/loads a package with an empty config group cleanly. -->
  <group name="General">
  </group>
</kcfg>
